{
  "name": "@docker-watch/cli",
  "version": "1.0.0",
  "description": "CLI for docker-watch",
  "main": "lib/index",
  "author": "Adam Billard",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && node lib/index.js",
    "test": "npm run build && node lib/index.js -d -o \"start\" -C \"echo Hello World\"",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^13.1.0"
  }
}
